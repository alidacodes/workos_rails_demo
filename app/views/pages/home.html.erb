<div>
  <h1 class="font-bold text-4xl">AlidaCodes Portal</h1>
  <span>To do: add a dialog box prompting users to log in?</span>
  <% if authenticated? %>
    <%= session[:user_id] %>
    <%= session[:user_idp_id]%>
  <% end %>
</div>
