<div>
  <h1 class="font-bold text-4xl">AlidaCodes Portal</h1>

  <%= link_to "Sign In with WorkOS", login_path, data: { turbo: false } unless authenticated? %>
  <%= link_to "Sign Out", logout_path, data: { turbo_method: :delete } if authenticated? %>

</div>
